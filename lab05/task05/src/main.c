//Без допомоги зовнішніх бібліотек, отримати корінь заданого числа.
int main() {
        const int num = 100 ;//Задане число.
        int root = 0 ;//Корінь числа.
//Знаходження кореню числа(Працює лише тоді, кори корінь ціле число):
        for (int i=1; i < num; i++) {
                if ( i * i == num) {
                         root = i ;
                }
        }
        return 0;
}
