int main() {
        const int numb = 2 ; //Число
        int numb2 = numb * numb ; //Число в квадраті
        int numb4 = numb2 * numb2 ; //Число в четвертій степені(операції - строки 4 і 5)
        int numb6 = numb4 * numb2 ; //Число в шостій степені(операції - строки 4, 5 і 6)
        int numb8 = numb4 * numb4 ; //Число в восьмій степені(операції - строки 4 ,5 і 7)
        int numb10 = numb8 * numb2 ; //Число в десятій степені(операції - строки 4,5,7 і 8)
        return 0;
}
